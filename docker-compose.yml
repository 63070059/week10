version: "3"
services:
  backend:
    build: .
    image: fast-api-docker_lab5/fast-api
    ports:
      - "8088:80"
    container_name: fast-api_container
  frontend:
    build: ./frontend
    image: frontend/frontend
    ports:
      - "5000:3000"
    container_name: frontend_container