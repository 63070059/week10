version: "3"
services:
  backend:
    build: .
    image: fast-api-docker_lab5
    ports:
      - "8088:80"
    container_name: fast-api_container
  frontend:
    build: ./frontend
    image: frontend
    ports:
      - "5000:3000"
    container_name: frontend_container